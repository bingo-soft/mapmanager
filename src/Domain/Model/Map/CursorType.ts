export enum CursorType {
    Default = "default",
    Pointer = "pointer",
    ZoomIn = "zoom_in", 
    ZoomOut = "zoom_out",
    AttributeByArea = "attribute_by_area",
    AttributeByClick = "attribute_by_click",
    SelectByArea = "select_by_area",
    SelectByClick = "select_by_click",
    SelectMultipleFeatures = "select_multiple_features",
    SelectOnMultipleLayers = "select_on_multiple_layers"
}

export default CursorType;