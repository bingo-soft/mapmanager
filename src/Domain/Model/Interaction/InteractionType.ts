enum InteractionType {
    Normal = "normal",
    Draw = "draw",
    Select = "select",
    Zoom = "zoom",
    Modify = "modify",
    Transform = "transform",
    Measure = "measure",
    MapCoordinates = "mapcoordinates"
}

export default InteractionType;