{
    "name": "example",
    "version": "1.0.0",
    "description": "",
    "main": "index.html",
    "scripts": {
        "serve": "mkdir -p dist && cp -r -f assets dist && parcel serve index.html --host localhost --port 8081",
        "build": "parcel build --public-url . index.html && cp -r -f assets dist"
    },
    "author": "BingoSoft",
    "license": "ISC",
    "devDependencies": {
        "parcel-bundler": "^1.12.4",
        "typescript": "^4.2.2"
    }
}
