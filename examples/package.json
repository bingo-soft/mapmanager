{
    "name": "example",
    "version": "1.0.0",
    "description": "",
    "main": "index.html",
    "scripts": {
        "serve": "mkdirp dist && copyfiles -f assets/* dist && parcel serve index.html --host localhost --port 8181",
        "build": "parcel build --public-url . index.html && copyfiles -f assets/* dist"
    },
    "author": "Bingo-Soft",
    "license": "MIT",
    "dependencies": {
        "@types/ol": "^6.4.2",
        "axios": "^0.18.1",
        "ol": "^6.5.0",
        "ol-ext": "^3.1.17"
    },
    "devDependencies": {
        "copyfiles": "^2.4.1",
		"mkdirp": "^1.0.4",
        "parcel-bundler": "^1.12.4",
        "typescript": "^4.2.2"
    }
}
